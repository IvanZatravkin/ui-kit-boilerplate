export const justifyContent = ({
  justifyContent = "initial",
}: {
  justifyContent?: "flex-start" | "flex-end" | "center" | "space-between" | "space-around" | "initial";
}) => ({ justifyContent });
