export const height = ({
  height,
  minHeight,
  maxHeight,
}: {
  height?: number | string;
  minHeight?: number | string;
  maxHeight?: number | string;
}) => ({
  height,
  minHeight,
  maxHeight,
});
